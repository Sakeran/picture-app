const request = require('supertest');
const app = require('../../app');
const passportStub = require('passport-stub');
const Post = require('../../models/Post');
let db;


describe('Post endpoint', () => {
  xit('placeholder', (done) => done());
})
